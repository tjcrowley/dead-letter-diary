---
phase: 04-offline-pwa
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - apps/web/lib/db.ts
  - apps/web/lib/sync.ts
  - apps/web/components/SyncStatus.tsx
  - apps/web/app/write/page.tsx
  - apps/web/lib/__tests__/sync.test.ts
  - apps/web/components/__tests__/SyncStatus.test.tsx
autonomous: true
requirements:
  - OFFLINE-01
  - OFFLINE-02
  - OFFLINE-03

must_haves:
  truths:
    - "User can write entries while offline — they save to IndexedDB and the outbox queues them for server sync"
    - "When connection restores, outbox flushes all pending entries to the server in queuedAt order"
    - "Sync status indicator shows 'Synced' when outbox is empty, 'Saving...' during active save, and 'Offline — N entries pending' when offline with queued entries"
    - "Outbox entries persist across page refresh (IndexedDB, not memory)"
  artifacts:
    - path: "apps/web/lib/db.ts"
      provides: "Dexie DB bumped to version 2 with outbox table alongside drafts"
      contains: "outbox"
    - path: "apps/web/lib/sync.ts"
      provides: "getSyncStatus() and flushOutbox() functions; online event listener registration"
      exports: ["getSyncStatus", "flushOutbox", "registerSyncListener", "queueForSync"]
    - path: "apps/web/components/SyncStatus.tsx"
      provides: "React component rendering sync state string with live updates via useLiveQuery"
  key_links:
    - from: "apps/web/lib/sync.ts"
      to: "apps/web/lib/db.ts"
      via: "db.outbox.count() and db.outbox.orderBy('queuedAt').toArray()"
      pattern: "db\\.outbox"
    - from: "apps/web/app/write/page.tsx"
      to: "apps/web/lib/sync.ts"
      via: "queueForSync(entry) called after successful local encrypt+save"
      pattern: "queueForSync"
    - from: "apps/web/lib/sync.ts"
      to: "window online event"
      via: "window.addEventListener('online', flushOutbox)"
      pattern: "addEventListener.*online"
---

<objective>
Extend the Dexie database with an outbox table, implement the sync queue (outbox pattern with flush-on-online + Background Sync enhancement), and wire the SyncStatus indicator into the write surface.

Purpose: OFFLINE-01 is already partially satisfied by Phase 3's Dexie drafts table, but entries need a sync queue to reach the server after going offline. This plan closes the gap: entries written offline accumulate in the outbox and flush when connectivity returns.
Output: `lib/sync.ts` with sync logic, `lib/db.ts` at version 2 with outbox table, `SyncStatus` component, and the write page wired to queue entries.
</objective>

<execution_context>
@/Users/darren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-offline-pwa/04-RESEARCH.md
@.planning/phases/04-offline-pwa/04-01-SUMMARY.md

<interfaces>
<!-- From apps/web/lib/db.ts (Phase 3 output, version 1) -->
export interface DraftEntry {
  id: string;
  ciphertext: ArrayBuffer;
  iv: Uint8Array;
  aad: Uint8Array;
  wordCount: number;
  updatedAt: number;
}
export const db: DeadLetterDiaryDB  // version 1, drafts only
export function saveDraft(draft: DraftEntry): Promise<void>
export function loadDraft(id: string): Promise<DraftEntry | undefined>
export function loadLatestDraft(): Promise<DraftEntry | undefined>

<!-- From apps/web/lib/api.ts (Phase 3, assumed shape) -->
// POST /api/entries — submits encrypted entry to server
// Called with: { ciphertext, iv, aad, wordCount }

<!-- From apps/web/lib/sync.ts (NEW — define here) -->
export interface OutboxEntry {
  id: string;           // same as DraftEntry.id
  ciphertext: ArrayBuffer;
  iv: Uint8Array;
  aad: Uint8Array;
  wordCount: number;
  queuedAt: number;
  attempts: number;
}

export type SyncStatus =
  | { state: "synced" }
  | { state: "saving" }
  | { state: "offline"; pendingCount: number };
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Dexie DB to version 2 (outbox table) and implement sync.ts</name>
  <files>
    apps/web/lib/db.ts,
    apps/web/lib/sync.ts,
    apps/web/lib/__tests__/sync.test.ts
  </files>
  <behavior>
    - Test: After DB construction, `db.outbox` table exists (not undefined)
    - Test: `queueForSync(entry)` adds an OutboxEntry to `db.outbox` with `attempts: 0` and `queuedAt` set to a recent timestamp
    - Test: `flushOutbox(submitFn)` calls submitFn for each entry in queuedAt order, then deletes the entry from outbox on success
    - Test: `flushOutbox(submitFn)` increments `attempts` and keeps the entry when submitFn throws
    - Test: `getSyncStatus(false)` returns `{ state: "synced" }` when outbox is empty and navigator.onLine is true
    - Test: `getSyncStatus(true)` returns `{ state: "saving" }` regardless of outbox state
    - Test: `getSyncStatus(false)` returns `{ state: "offline", pendingCount: N }` when navigator.onLine is false and outbox has N entries
    - Test: `getSyncStatus(false)` returns `{ state: "saving" }` when online but outbox has entries (pending flush)
  </behavior>
  <action>
    **Update apps/web/lib/db.ts:**
    Add `OutboxEntry` interface export. Bump DB to version 2: add `this.version(2).stores({ drafts: "id, updatedAt", outbox: "id, queuedAt" })` alongside the existing `this.version(1).stores({ drafts: "id, updatedAt" })`. Add `outbox!: Table<OutboxEntry, string>` property. Export `OutboxEntry` interface.

    The version 1 declaration must remain unchanged (Dexie migration requires all versions declared). Version 2 re-declares drafts with same schema plus adds outbox.

    **Create apps/web/lib/sync.ts:**

    Export `OutboxEntry` re-export (or define here and import in db.ts — pick one source of truth: define `OutboxEntry` in `sync.ts`, import in `db.ts`).

    Export `SyncStatus` type as defined in interfaces above.

    Export `async function queueForSync(entry: DraftEntry): Promise<void>`:
    - Converts DraftEntry to OutboxEntry: same fields + `queuedAt: Date.now()`, `attempts: 0`
    - Calls `db.outbox.put(outboxEntry)`

    Export `async function flushOutbox(submitFn: (entry: OutboxEntry) => Promise<void>): Promise<void>`:
    - Fetches `await db.outbox.orderBy("queuedAt").toArray()`
    - Iterates in order: try `await submitFn(entry)`, then `await db.outbox.delete(entry.id)` on success; on throw, `await db.outbox.update(entry.id, { attempts: entry.attempts + 1 })`
    - Non-throwing: catch errors per entry, continue processing remaining entries

    Export `async function getSyncStatus(isSaving: boolean): Promise<SyncStatus>`:
    - If `isSaving` return `{ state: "saving" }`
    - Count `pendingCount = await db.outbox.count()`
    - If `!navigator.onLine && pendingCount > 0` return `{ state: "offline", pendingCount }`
    - If `pendingCount > 0` return `{ state: "saving" }` (online but not yet flushed)
    - Return `{ state: "synced" }`

    Export `function registerSyncListener(submitFn: (entry: OutboxEntry) => Promise<void>): () => void`:
    - Adds `window.addEventListener("online", handler)` where handler calls `flushOutbox(submitFn)`
    - If `"sync" in ServiceWorkerRegistration.prototype` is true (Chromium Background Sync), also register a Background Sync tag: navigator.serviceWorker.ready.then(reg => reg.sync.register("outbox-flush")) — wrapped in try/catch (non-fatal)
    - Returns cleanup function that calls `window.removeEventListener("online", handler)`

    **Write apps/web/lib/__tests__/sync.test.ts:**
    Use `fake-indexeddb` (already installed) to mock IndexedDB. Import `IDBFactory` from `fake-indexeddb` and set `globalThis.indexedDB = new IDBFactory()` before each test (reset between tests). Mock `navigator.onLine` via `Object.defineProperty(navigator, "onLine", { value: true/false, configurable: true, writable: true })`. Write all 8 behavior tests above.
  </action>
  <verify>
    <automated>cd /Users/darren/.openclaw/workspace/dead-letter-diary/apps/web && npx vitest run lib/__tests__/sync.test.ts</automated>
  </verify>
  <done>
    - sync.test.ts: all 8 behavior tests pass
    - db.ts: version 2, outbox table declared
    - sync.ts: queueForSync, flushOutbox, getSyncStatus, registerSyncListener exported
    - Existing db.test.ts still passes (no regression on drafts table)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SyncStatus component and write page integration</name>
  <files>
    apps/web/components/SyncStatus.tsx,
    apps/web/app/write/page.tsx,
    apps/web/components/__tests__/SyncStatus.test.tsx
  </files>
  <behavior>
    - Test: SyncStatus renders "Synced" text when state is "synced"
    - Test: SyncStatus renders "Saving..." text when state is "saving"
    - Test: SyncStatus renders "Offline — 3 entries pending" when state is { state: "offline", pendingCount: 3 }
    - Test: SyncStatus renders "Offline — 1 entry pending" (singular) when pendingCount is 1
  </behavior>
  <action>
    **Create apps/web/components/SyncStatus.tsx** (`"use client"`):
    Uses Dexie's `useLiveQuery` hook to reactively count outbox entries. Internally calls `getSyncStatus(isSaving)` and re-evaluates when outbox changes.

    Props: `{ isSaving: boolean }`.

    Implementation:
    ```typescript
    "use client";
    import { useLiveQuery } from "dexie-react-hooks";
    import { db } from "@/lib/db";
    import { getSyncStatus, type SyncStatus } from "@/lib/sync";
    import { useState, useEffect } from "react";

    export function SyncStatus({ isSaving }: { isSaving: boolean }) {
      const pendingCount = useLiveQuery(() => db.outbox.count(), []) ?? 0;
      const [isOnline, setIsOnline] = useState(
        typeof navigator !== "undefined" ? navigator.onLine : true
      );

      useEffect(() => {
        const onOnline = () => setIsOnline(true);
        const onOffline = () => setIsOnline(false);
        window.addEventListener("online", onOnline);
        window.addEventListener("offline", onOffline);
        return () => {
          window.removeEventListener("online", onOnline);
          window.removeEventListener("offline", onOffline);
        };
      }, []);

      let label: string;
      if (isSaving) {
        label = "Saving...";
      } else if (!isOnline && pendingCount > 0) {
        label = `Offline — ${pendingCount} ${pendingCount === 1 ? "entry" : "entries"} pending`;
      } else if (pendingCount > 0) {
        label = "Saving...";
      } else {
        label = "Synced";
      }

      return (
        <div
          aria-live="polite"
          style={{ fontSize: "0.75rem", color: "#888", padding: "4px 8px" }}
        >
          {label}
        </div>
      );
    }
    ```

    Check if `dexie-react-hooks` is installed. If not:
    ```bash
    npm install dexie-react-hooks --workspace=@dead-letter-diary/web
    ```

    **Update apps/web/app/write/page.tsx:**
    Read the existing write page first to understand its structure. Add the following integrations:
    1. Import `SyncStatus` from `@/components/SyncStatus`
    2. Import `queueForSync`, `registerSyncListener` from `@/lib/sync`
    3. Import the API submit function from `@/lib/api`
    4. Track `isSaving` state (boolean, set true when debounced save fires, false when save completes)
    5. In the auto-save callback (after `saveDraft()`): also call `queueForSync(draftEntry)` then attempt immediate server submit; on network failure (catch), leave in outbox (flush will retry on online event)
    6. Register `registerSyncListener` in a `useEffect` on mount; store the cleanup function and call it on unmount
    7. Render `<SyncStatus isSaving={isSaving} />` in the write UI (e.g., below the word count display)

    For the server submit function within the write page: wrap the call to the entries API in a try/catch. On success, delete the outbox entry for that ID (`db.outbox.delete(entryId)`). On failure, leave it for the sync listener to retry.

    **Write apps/web/components/__tests__/SyncStatus.test.tsx:**
    Mock `dexie-react-hooks` `useLiveQuery` to return a specific count value. Mock `navigator.onLine`. Render `<SyncStatus isSaving={false/true} />` and assert text content. Use `@testing-library/react` render + `screen.getByText`. Test all 4 behavior cases above.
  </action>
  <verify>
    <automated>cd /Users/darren/.openclaw/workspace/dead-letter-diary/apps/web && npx vitest run components/__tests__/SyncStatus.test.tsx lib/__tests__/sync.test.ts</automated>
  </verify>
  <done>
    - SyncStatus.test.tsx: all 4 label tests pass
    - sync.test.ts: still all passing (no regression)
    - SyncStatus component renders correct label for all three states including singular/plural
    - write/page.tsx calls queueForSync after local save and registers the online flush listener
    - `npx vitest run` passes entire suite
  </done>
</task>

</tasks>

<verification>
Full suite: `cd /Users/darren/.openclaw/workspace/dead-letter-diary/apps/web && npx vitest run`

All sync and SyncStatus tests green. Existing crypto, db, word-count tests still pass (no regression on Phase 3 work). db.ts version 2 migration does not break existing drafts tests.
</verification>

<success_criteria>
1. `npx vitest run` green including sync.test.ts and SyncStatus.test.tsx
2. db.ts exports OutboxEntry and has version 2 schema with outbox table
3. sync.ts exports getSyncStatus, flushOutbox, queueForSync, registerSyncListener
4. SyncStatus component renders "Synced" / "Saving..." / "Offline — N entries pending"
5. write/page.tsx calls queueForSync after local save and registers online flush listener
</success_criteria>

<output>
After completion, create `.planning/phases/04-offline-pwa/04-02-SUMMARY.md` following the summary template.
</output>
