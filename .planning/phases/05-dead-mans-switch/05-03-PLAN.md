---
phase: 05-dead-mans-switch
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/api/src/routes/deadline.ts
  - apps/api/src/routes/__tests__/deadline.test.ts
  - apps/web/components/GraceDayButton.tsx
  - apps/web/components/AkrasiaSettings.tsx
  - apps/web/components/__tests__/GraceDayButton.test.tsx
autonomous: true
requirements:
  - DMS-09
  - DMS-10

must_haves:
  truths:
    - "Grace day invocation extends deadline_at by 24h and records grace_used_at"
    - "Grace budget returns 429 when budget=0 within the same 7-day window"
    - "Grace budget resets to 1 automatically when grace_used_at is > 7 days ago (or null)"
    - "Weekly grace budget is visible in the UI"
    - "Weakening a commitment (lower word count or longer window) sets pending_effective_at = now + 7 days — not applied immediately"
    - "Strengthening a commitment (higher word count or shorter window) applies immediately with no pending period"
    - "GET /api/deadline response includes pending_* fields so client can show 'change pending in N days' messaging"
  artifacts:
    - path: "apps/api/src/routes/deadline.ts"
      provides: "POST /api/deadline/grace endpoint + Akrasia logic in POST /api/deadline/settings (already scaffolded in Plan 01, this plan adds full grace route and strengthens Akrasia tests)"
    - path: "apps/api/src/routes/__tests__/deadline.test.ts"
      provides: "Full grace day tests: budget=0 returns 429, budget resets after 7 days, deadline extended by 24h"
    - path: "apps/web/components/GraceDayButton.tsx"
      provides: "Button showing remaining grace budget (0 or 1); invokes POST /api/deadline/grace; refreshes DeadlineBanner"
    - path: "apps/web/components/AkrasiaSettings.tsx"
      provides: "Settings form for word_minimum and window_hours; displays pending change and effective date when a weakening is queued"
    - path: "apps/web/components/__tests__/GraceDayButton.test.tsx"
      provides: "Unit tests for GraceDayButton error states and disabled-when-budget-0 behavior"
  key_links:
    - from: "apps/web/components/GraceDayButton.tsx"
      to: "/api/deadline/grace"
      via: "POST fetch on button click"
      pattern: "fetch.*deadline/grace"
    - from: "apps/web/components/AkrasiaSettings.tsx"
      to: "/api/deadline/settings"
      via: "POST fetch on form submit"
      pattern: "fetch.*deadline/settings"
    - from: "apps/api/src/routes/deadline.ts"
      to: "deadline_state"
      via: "SELECT ... FOR UPDATE in grace and settings transactions"
      pattern: "FOR UPDATE"
---

<objective>
Implement grace days and Akrasia Horizon as complete API endpoints with full test coverage, plus the client UI components (GraceDayButton and AkrasiaSettings) that expose these mechanics to the user.

Purpose: Plans 01 and 02 scaffold the routes and Akrasia column logic. This plan finalizes the POST /api/deadline/grace route and adds full unit test coverage for all grace day edge cases and Akrasia boundary conditions — plus the UI components that make these mechanics usable.

Output: Grace day route fully tested (budget exhausted, reset, extension logic). AkrasiaSettings form distinguishing strengthen vs weaken. GraceDayButton showing budget.
</objective>

<execution_context>
@/Users/darren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dead-mans-switch/05-RESEARCH.md
@.planning/phases/05-dead-mans-switch/05-01-SUMMARY.md
@apps/api/src/routes/deadline.ts
@apps/api/src/routes/__tests__/deadline.test.ts
@apps/api/src/test-helpers/index.ts

<interfaces>
<!-- From Plan 01 output — deadline.ts route already exists -->
<!-- This plan adds POST /api/deadline/grace and deepens test coverage -->

Grace day DB columns (already in schema):
```sql
deadline_state.grace_used_at TIMESTAMPTZ  -- last time grace was invoked
deadline_state.grace_budget INT DEFAULT 1  -- current weekly allowance
```

Akrasia pending columns (already in schema):
```sql
deadline_state.pending_window_hours INT
deadline_state.pending_word_minimum INT
deadline_state.pending_effective_at TIMESTAMPTZ
```

Grace logic (from research Pattern 3):
```typescript
import { DateTime } from 'luxon';
const now = DateTime.utc();
const lastGrace = row.grace_used_at ? DateTime.fromJSDate(row.grace_used_at, { zone: 'utc' }) : null;
const budgetReset = !lastGrace || now.diff(lastGrace, 'days').days >= 7;
const effectiveBudget = budgetReset ? 1 : row.grace_budget;
if (effectiveBudget < 1) return reply.status(429).send({ error: 'Grace budget exhausted for this week' });
// extend deadline by 24h, record grace_used_at = now, grace_budget = 0
```

Akrasia logic (from Plan 01 — already in POST /api/deadline/settings):
- weakening: (newWordMin < currentWordMin) OR (newWindowHours > currentWindowHours) → sets pending_* with pending_effective_at = now + 7d
- strengthening: (newWordMin > currentWordMin) OR (newWindowHours < currentWindowHours) → writes directly to word_minimum / window_hours
- Mixed (weaker on one axis, stronger on other): treat each axis independently — stronger applies immediately, weaker goes pending

GET /api/deadline response (from Plan 01) already includes pending_* fields — client can read them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Grace day route and full test coverage</name>
  <files>
    apps/api/src/routes/deadline.ts
    apps/api/src/routes/__tests__/deadline.test.ts
  </files>
  <behavior>
    POST /api/deadline/grace (new route in deadline.ts, requires auth):
    - SELECT deadline_state WHERE user_id=$1 FOR UPDATE in a transaction (DMS-07 — prevent race with poller)
    - Return 404 if no deadline_state row
    - Return 409 if state != 'active' (cannot invoke grace on a pending_wipe or wiped diary)
    - Compute effectiveBudget using Luxon: if grace_used_at is NULL or diff >= 7 days, reset to 1; else use grace_budget
    - Return 429 with { error: 'Grace budget exhausted for this week' } if effectiveBudget < 1
    - UPDATE deadline_state SET deadline_at = deadline_at + INTERVAL '24 hours', grace_used_at = now(), grace_budget = 0, updated_at = now() WHERE user_id=$1
    - COMMIT and return 200 with { new_deadline_at, grace_budget: 0, message: 'Grace day applied. Deadline extended by 24 hours.' }

    deadline.test.ts additions:
    - POST /api/deadline/grace returns 401 without auth
    - POST /api/deadline/grace returns 404 when no deadline_state row
    - POST /api/deadline/grace returns 409 when state='pending_wipe'
    - POST /api/deadline/grace returns 429 when grace_budget=0 AND grace_used_at within last 7 days
    - POST /api/deadline/grace returns 200 and extends deadline when budget=0 but grace_used_at > 7 days ago (budget reset triggers)
    - POST /api/deadline/grace returns 200 when grace_budget=1, sets grace_budget=0 in UPDATE
    - POST /api/deadline/settings: weakening word_minimum sets pending_word_minimum + pending_effective_at ~7d from now, does NOT update word_minimum directly
    - POST /api/deadline/settings: strengthening word_minimum updates word_minimum immediately, no pending fields set
    - POST /api/deadline/settings: mixed (weaker window, stronger word_minimum) — applies word_minimum immediately and sets pending_window_hours
  </behavior>
  <action>
    Append the POST /api/deadline/grace handler to apps/api/src/routes/deadline.ts. Import pool.connect() pattern for the transaction (same FOR UPDATE pattern as checkin route from Plan 01). Use Luxon DateTime for the 7-day budget reset check (already installed from Plan 01).

    Expand apps/api/src/routes/__tests__/deadline.test.ts with the grace day describe block and the additional Akrasia test cases listed above. Use buildTestApp + mockPool with a stateful queryFn that tracks SQL calls and returns appropriate mock rows per test case.

    For the budget-reset test: set grace_used_at to 8 days ago (Date.now() - 8*24*3600*1000) in the mock row. Verify the UPDATE SQL is called (budget reset path). For the budget-exhausted test: set grace_used_at to 1 day ago and grace_budget=0 — verify 429.
  </action>
  <verify>
    <automated>cd /Users/darren/.openclaw/workspace/dead-letter-diary/apps/api && npx vitest run src/routes/__tests__/deadline.test.ts</automated>
  </verify>
  <done>All deadline route tests pass including grace day edge cases (429, reset, 409 state guard) and Akrasia boundary tests (weaken pending, strengthen immediate, mixed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GraceDayButton and AkrasiaSettings UI components</name>
  <files>
    apps/web/components/GraceDayButton.tsx
    apps/web/components/AkrasiaSettings.tsx
    apps/web/components/__tests__/GraceDayButton.test.tsx
  </files>
  <behavior>
    GraceDayButton.test.tsx tests (write tests BEFORE implementation):
    - Renders disabled button with label "Grace day used this week" when graceBudget=0
    - Renders enabled button with label "Invoke Grace Day (1 remaining this week)" when graceBudget=1
    - Shows inline error "Grace budget exhausted for this week" when server returns 429
    - Shows inline error "Cannot invoke grace day — deadline has already passed" when server returns 409
    - Calls onGraceUsed() callback on successful 200 response
    - Button is disabled (shows "Applying...") while fetch is in flight

    Use vitest + @testing-library/react. Mock fetch with vi.stubGlobal('fetch', ...) per test case to simulate 200, 429, and 409 responses. Use userEvent.click() for button interactions.
  </behavior>
  <action>
    First write apps/web/components/__tests__/GraceDayButton.test.tsx with all 6 test cases listed in the behavior block. Run tests — they must fail (RED). Then implement.

    Create apps/web/components/GraceDayButton.tsx as a 'use client' component.
    - Props: { graceBudget: number; onGraceUsed?: () => void } (caller passes budget from /api/deadline response)
    - Render: If graceBudget > 0: enabled button labeled "Invoke Grace Day (1 remaining this week)"; if 0: disabled button labeled "Grace day used this week".
    - On click: POST /api/deadline/grace with credentials:'include'. On success: call onGraceUsed?.() so parent can refetch DeadlineBanner. On 429: show inline error "Grace budget exhausted for this week". On 409: show "Cannot invoke grace day — deadline has already passed".
    - Loading state: disable button while request in flight, show "Applying..." label.

    Create apps/web/components/AkrasiaSettings.tsx as a 'use client' component.
    - Props: { currentSettings: { word_minimum: number; window_hours: number; pending_word_minimum?: number; pending_window_hours?: number; pending_effective_at?: string } }
    - Render a form with two number inputs: word minimum (range 25–500) and window hours (range 12–168 which is 7 days in hours).
    - Below each input, if a pending change exists for that axis: show "Change to N pending — effective [date]" in muted text (using pending_effective_at formatted as a human-readable local date).
    - On submit: POST /api/deadline/settings with { word_minimum, window_hours } and credentials:'include'. On success: show "Settings saved." If the server applies a change as pending (detected by response including pending_effective_at), show "Weakening change queued — will apply in 7 days."
    - Do NOT auto-detect strengthen vs weaken on the client — let the server determine and reflect back in the response.
  </action>
  <verify>
    <automated>cd /Users/darren/.openclaw/workspace/dead-letter-diary/apps/web && npx vitest run components/__tests__/GraceDayButton.test.tsx</automated>
  </verify>
  <done>GraceDayButton.test.tsx: all 6 tests pass (budget=0 disabled, 429 error, 409 error, success callback, loading state). GraceDayButton and AkrasiaSettings components exist with correct prop interfaces and POST /api/deadline/* fetch calls.</done>
</task>

</tasks>

<verification>
Run full suites for both apps to confirm no regressions:
`cd /Users/darren/.openclaw/workspace/dead-letter-diary/apps/api && npx vitest run`
`cd /Users/darren/.openclaw/workspace/dead-letter-diary/apps/web && npx vitest run`

Confirm deadline.ts now has three routes: GET /api/deadline, POST /api/deadline/settings, POST /api/deadline/checkin, POST /api/deadline/grace.

Confirm POST /api/deadline/grace uses FOR UPDATE (not a plain UPDATE) — the row-level lock is required by DMS-07.

Confirm AkrasiaSettings renders "Change to N pending" messaging when pending_* fields are set.

Confirm GraceDayButton.test.tsx tests all error states (429, 409) and disabled-when-budget-0 behavior.
</verification>

<success_criteria>
- All deadline.test.ts cases pass: grace 429, grace budget reset, grace 409, weaken pending, strengthen immediate, mixed axis
- POST /api/deadline/grace acquires FOR UPDATE lock (verified in test by checking BEGIN + FOR UPDATE SQL calls in mock)
- GraceDayButton.test.tsx: 6 tests pass covering budget=0 disabled, 429/409 error states, success callback, loading state
- GraceDayButton disables when graceBudget=0 with correct label
- AkrasiaSettings shows pending change with effective date
- Full API and web vitest suites green
</success_criteria>

<output>
After completion, create `.planning/phases/05-dead-mans-switch/05-03-SUMMARY.md`
</output>
