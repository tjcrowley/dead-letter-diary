---
phase: 06-wipe-and-ceremony
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - apps/web/app/sw.ts
  - apps/web/lib/wipe.ts
  - apps/web/lib/__tests__/wipe.test.ts
  - apps/web/app/wiped/page.tsx
  - apps/web/app/wiped/__tests__/page.test.tsx
  - apps/web/components/PanicEncryptButton.tsx
  - apps/web/components/__tests__/PanicEncryptButton.test.tsx
  - apps/web/components/DeadlineBanner.tsx
autonomous: true
requirements:
  - WIPE-02
  - WIPE-03
  - WIPE-05
  - WIPE-06

must_haves:
  truths:
    - "Service Worker push handler intercepts type:'wipe' payload, clears all Cache Storage and IndexedDB, shows wipe notification, and navigates open clients to /wiped"
    - "performClientWipe() deletes Dexie DB, clears Cache Storage, and clears session cookie from window context"
    - "/wiped page renders diary title + epitaph (if set); makes no calls to /api/entries or any crypto endpoint"
    - "DeadlineBanner detects state='wiped' and triggers performClientWipe() then router.replace('/wiped') instead of returning null"
    - "PanicEncryptButton shows typed confirmation dialog (user must type DESTROY); only calls POST /api/wipe/panic after confirmation"
  artifacts:
    - path: "apps/web/lib/wipe.ts"
      provides: "performClientWipe() exported async function"
      exports: ["performClientWipe"]
    - path: "apps/web/app/sw.ts"
      provides: "push handler extended with type:'wipe' branch"
    - path: "apps/web/app/wiped/page.tsx"
      provides: "/wiped route — wipe ceremony screen"
    - path: "apps/web/components/PanicEncryptButton.tsx"
      provides: "Two-step confirmation + POST /api/wipe/panic caller"
    - path: "apps/web/components/DeadlineBanner.tsx"
      provides: "Wipe detection on every poll — calls performClientWipe + redirect"
    - path: "apps/web/lib/__tests__/wipe.test.ts"
      provides: "Tests for performClientWipe and SW wipe handler behavior"
    - path: "apps/web/components/__tests__/PanicEncryptButton.test.tsx"
      provides: "Tests for confirmation dialog gate and API call"
    - path: "apps/web/app/wiped/__tests__/page.test.tsx"
      provides: "Tests that /wiped renders only title/epitaph"
  key_links:
    - from: "apps/web/app/sw.ts push handler"
      to: "indexedDB.deleteDatabase('DeadLetterDiary')"
      via: "SW wipe branch, navigate clients first then delete IDB"
      pattern: "type.*wipe"
    - from: "apps/web/components/DeadlineBanner.tsx"
      to: "apps/web/lib/wipe.ts"
      via: "import performClientWipe; call on state==='wiped'"
      pattern: "performClientWipe"
    - from: "apps/web/app/wiped/page.tsx"
      to: "GET /api/account/epitaph"
      via: "fetch on mount to display epitaph"
      pattern: "api/account/epitaph"
---

<objective>
Build all client-side wipe ceremony pieces: SW push handler (type:'wipe'), performClientWipe() lib, /wiped page, DeadlineBanner wipe guard, and PanicEncryptButton with typed confirmation.

Purpose: The server wipe (Plan 01) is complete but invisible to the client. This plan makes the wipe observable and irreversible on the browser side — clearing all local data and presenting the ceremony screen.

Output: performClientWipe() lib, updated sw.ts, /wiped page, updated DeadlineBanner, PanicEncryptButton component — all tested with Vitest in happy-dom environment.
</objective>

<execution_context>
@/Users/darren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-wipe-and-ceremony/06-RESEARCH.md
@.planning/phases/06-wipe-and-ceremony/06-VALIDATION.md
@.planning/phases/06-wipe-and-ceremony/06-01-SUMMARY.md

@apps/web/app/sw.ts
@apps/web/components/DeadlineBanner.tsx
@apps/web/lib/db.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/web/app/sw.ts (current push handler to be extended):
```typescript
// Current push handler — extend to detect type:'wipe' BEFORE falling through to existing notification logic
self.addEventListener("push", (event: PushEvent) => {
  const data = event.data?.json() as {
    title?: string;
    body?: string;
    data?: Record<string, unknown>;
  };
  // Currently: shows notification for all pushes
  // Phase 6: add type check — if data.type === 'wipe', run wipe ceremony and return early
});
```

From apps/web/lib/db.ts (Dexie database name):
```typescript
// Database is named 'DeadLetterDiary' — use this string in indexedDB.deleteDatabase()
// db.delete() is also available but SW context cannot import Dexie modules
// In SW context: use raw indexedDB.deleteDatabase('DeadLetterDiary') with onblocked resolution
```

From apps/web/components/DeadlineBanner.tsx (wipe stub to replace):
```typescript
// Line 103: if (deadlineState.state === "wiped") return null;
// Replace with: call performClientWipe() then router.replace('/wiped')
// Need: import { useRouter } from 'next/navigation'
// Need: import { performClientWipe } from '@/lib/wipe'
```

Dead letter diary cookie format (from Phase 2):
```typescript
// Cookie name: 'session', httpOnly, Secure, SameSite=Strict
// Clear with: document.cookie = 'session=; Max-Age=0; path=/; Secure; SameSite=Strict'
```

Vitest config (web) — happy-dom environment, @/ alias mapped:
```
cd apps/web && npx vitest run
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: performClientWipe() lib + SW type:'wipe' push handler (WIPE-02, WIPE-06)</name>
  <files>apps/web/lib/wipe.ts, apps/web/lib/__tests__/wipe.test.ts, apps/web/app/sw.ts</files>
  <behavior>
    - performClientWipe(): calls db.delete() to drop the Dexie 'DeadLetterDiary' database; wraps in try/catch (already deleted = not an error)
    - performClientWipe(): clears Cache Storage via caches.keys() then Promise.all(keys.map(k => caches.delete(k))); guards with 'caches' in window check
    - performClientWipe(): clears session cookie: document.cookie = 'session=; Max-Age=0; path=/; Secure; SameSite=Strict'
    - SW push handler: checks if data?.type === 'wipe' BEFORE the existing notification display logic
    - SW wipe branch: (1) clear Cache Storage via caches.keys() + parallel delete, (2) navigate all open clients to /wiped BEFORE deleteDatabase to unblock open connections, (3) delete IndexedDB 'DeadLetterDiary' treating onblocked as resolve, (4) show wipe notification with requireInteraction:true, then return without falling through
    - Test: performClientWipe clears IDB (mock db.delete), clears caches (mock caches API), clears cookie
    - Test: performClientWipe does not throw if db.delete() rejects (already deleted)
    - Test: SW wipe handler navigates clients before deleteDatabase call (order matters — prevents onblocked deadlock)
  </behavior>
  <action>
    Create apps/web/lib/wipe.ts: import { db } from './db'; export async function performClientWipe(): Promise<void>. Implement the three-step cleanup described in behavior. The db.delete() call must be wrapped in try/catch — treat rejection as success (database already gone). Cache clearing only when 'caches' in window (SSR guard). Cookie clearing via document.cookie assignment.

    Update apps/web/app/sw.ts: In the push event listener, change the data type to include `type?: string`. At the TOP of the handler (before the existing notification logic), check: if (data?.type === 'wipe') { event.waitUntil((async () => { ... wipe ceremony ... })(); return; }. The wipe ceremony async block must: (1) caches.keys() + parallel delete, (2) const allClients = await self.clients.matchAll({ type: 'window' }); for each client: client.navigate('/wiped'), (3) await new Promise<void>((resolve) => { const req = indexedDB.deleteDatabase('DeadLetterDiary'); req.onsuccess = () => resolve(); req.onerror = () => resolve(); req.onblocked = () => resolve(); }), (4) await self.registration.showNotification('Dead Letter Diary', { body: 'Your diary has been permanently destroyed.', icon: '/icons/icon-192x192.png', requireInteraction: true }).

    Create apps/web/lib/__tests__/wipe.test.ts with Vitest. Mock db (vi.mock('../db', () => ({ db: { delete: vi.fn() } }))). Mock global caches API and document.cookie. Test performClientWipe behavior cases. For SW handler, test indirectly via the exported functions if SW is not easily testable; at minimum test performClientWipe thoroughly.

    Follow RED→GREEN cycle.
  </action>
  <verify>
    <automated>cd /Users/darren/.openclaw/workspace/dead-letter-diary/apps/web && npx vitest run lib/__tests__/wipe.test.ts</automated>
  </verify>
  <done>wipe.ts tests pass. performClientWipe exported. sw.ts has type:'wipe' branch that navigates before deleteDatabase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /wiped page + PanicEncryptButton + DeadlineBanner wipe guard (WIPE-03, WIPE-05, WIPE-06)</name>
  <files>apps/web/app/wiped/page.tsx, apps/web/app/wiped/__tests__/page.test.tsx, apps/web/components/PanicEncryptButton.tsx, apps/web/components/__tests__/PanicEncryptButton.test.tsx, apps/web/components/DeadlineBanner.tsx</files>
  <behavior>
    - /wiped page: "use client"; on mount, fetches GET /api/account/epitaph; renders diary title ("Dead Letter Diary") and epitaph below it if non-null; NO calls to /api/entries, /api/crypto, or any content endpoint; clears session cookie on mount (document.cookie = 'session=; Max-Age=0; path=/; Secure; SameSite=Strict')
    - /wiped page: gravestone aesthetic — centered, minimal, dark background or neutral; title is prominent; epitaph in smaller italic text; no navigation links, no buttons except to acknowledge
    - /wiped page test: renders "Dead Letter Diary" title; does not render any diary content; shows epitaph text when API returns one; shows nothing extra when epitaph is null
    - PanicEncryptButton: renders a button labeled "Panic Encrypt (destroy diary)"; on click, shows a confirmation dialog with "This will permanently destroy your diary. This cannot be undone." and an input requiring the user to type "DESTROY"
    - PanicEncryptButton: the confirm button in dialog is disabled until typed value === "DESTROY"; on confirm, calls POST /api/wipe/panic with credentials:'include'; on 200 response, calls performClientWipe() then router.replace('/wiped'); on non-200, shows error message in dialog
    - PanicEncryptButton test: dialog does not appear before button click; confirm button disabled when typed value is not 'DESTROY'; confirm button enabled when value is 'DESTROY'; POST /api/wipe/panic called on confirm; API call NOT made when dialog dismissed
    - DeadlineBanner: replace `if (deadlineState.state === "wiped") return null;` with a useEffect-driven check — when deadlineState.state === 'wiped', call performClientWipe().then(() => router.replace('/wiped')); import useRouter from 'next/navigation'
  </behavior>
  <action>
    Create apps/web/app/wiped/page.tsx: "use client". State: epitaph (string|null). useEffect on mount: (1) clear session cookie, (2) fetch('/api/account/epitaph', { credentials: 'include' }).then parse JSON, set epitaph state. Render: full-screen centered div with dark/neutral Tailwind styling. Large "Dead Letter Diary" heading. If epitaph non-null: italic paragraph below. CRITICAL: no imports from diary content modules, no fetch to entries/crypto endpoints.

    Create apps/web/app/wiped/__tests__/page.test.tsx: mock fetch for /api/account/epitaph returning { epitaph: 'some text' } and null cases. Use @testing-library/react render. Assert title present. Assert no fetch calls to forbidden endpoints.

    Create apps/web/components/PanicEncryptButton.tsx: "use client". State: showDialog (bool), typedValue (string), loading (bool), error (string|null). On button click: setShowDialog(true). Dialog rendered when showDialog: text description + input bound to typedValue + confirm button (disabled when typedValue !== 'DESTROY') + cancel button. On confirm: POST /api/wipe/panic, on 200 call performClientWipe() then router.replace('/wiped'), on error set error state.

    Create apps/web/components/__tests__/PanicEncryptButton.test.tsx: mock fetch and performClientWipe. Test dialog visibility, confirm button enabled/disabled, API call sequencing.

    Update apps/web/components/DeadlineBanner.tsx: add `import { useRouter } from 'next/navigation'`; add `import { performClientWipe } from '@/lib/wipe'`. Add `const router = useRouter()` at top of component. In useEffect or in the fetchDeadline callback, after updating state, check: if (data.state === 'wiped') { void performClientWipe().then(() => { router.replace('/wiped'); }); return; }. Remove the old `if (deadlineState.state === "wiped") return null;` line — redirect is now handled before render.

    Follow RED→GREEN for each new file.
  </action>
  <verify>
    <automated>cd /Users/darren/.openclaw/workspace/dead-letter-diary/apps/web && npx vitest run app/wiped/__tests__/page.test.tsx components/__tests__/PanicEncryptButton.test.tsx</automated>
  </verify>
  <done>All three test files pass. /wiped page exists and renders only title + epitaph. PanicEncryptButton requires typed DESTROY before API call. DeadlineBanner redirects to /wiped on wiped state. Full web suite green: `cd apps/web && npx vitest run`</done>
</task>

</tasks>

<verification>
Full web suite must pass:
`cd /Users/darren/.openclaw/workspace/dead-letter-diary/apps/web && npx vitest run`

Manual check: grep sw.ts for `type.*wipe`. Verify /wiped page has no import of entries or crypto libs.
</verification>

<success_criteria>
- performClientWipe() in apps/web/lib/wipe.ts: clears IDB + caches + session cookie
- sw.ts push handler: type:'wipe' branch navigates clients to /wiped, clears IDB and caches in SW context
- /app/wiped/page.tsx: renders only diary title and optional epitaph — no diary content
- PanicEncryptButton: typed 'DESTROY' confirmation before POST /api/wipe/panic
- DeadlineBanner: performClientWipe() + router.replace('/wiped') on wiped state (not null return)
- All apps/web vitest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-wipe-and-ceremony/06-02-SUMMARY.md` using the summary template.
</output>
